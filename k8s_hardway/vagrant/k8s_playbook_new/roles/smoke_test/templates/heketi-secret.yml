apiVersion: v1
kind: Secret
metadata:
  name: heketi-secret
  namespace: default
data:
  # base64 encoded password. E.g.: echo -n "My Secret" | base64
  key: TXkgU2VjcmV0
  type: kubernetes.io/glusterfs
