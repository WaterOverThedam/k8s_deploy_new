apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: {{glusterfs_sc}}
provisioner: kubernetes.io/glusterfs
parameters:
  resturl: "http://{{svc_ip.stdout}}"
  restauthenabled: "true"
  restuser: "admin"
  secretNamespace: "default"
  secretName: "heketi-secret"
  volumetype: "replicate:2"

